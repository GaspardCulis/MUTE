---

---

<div id="title-view"></div>
<script>
  import * as THREE from "three";
  import { PointerLockControls } from "three/addons/controls/PointerLockControls.js";
  import {
    createRenderer,
    createScene,
    addTitleText,
  } from "../lib/TitleRenderer.ts";
  import Font from "../lib/Font";

  const container = document.querySelector("#title-view") as HTMLDivElement;
  const w = container.clientWidth;
  const h = container.clientHeight;

  const renderer = createRenderer(container);
  const [scene, camera] = createScene(w, h);

  const controls = new PointerLockControls(camera, container);

  const font = await Font.load("minecraft-ten-v2");

  console.log(font);
  await addTitleText(scene, "minecraft", {
    font,
    texture: "cracked",
    type: "top",
  });
  await addTitleText(scene, "the wild update", {
    font,
    texture: "deep_dark",
    type: "bottom",
  });
  // ;)

  console.log(scene.children);

  function animate() {
    requestAnimationFrame(animate);
    renderer.render(scene, camera);
  }
  animate();
</script>
<style>
  #title-view {
    height: 480px;

    background-color: whitesmoke;
  }
</style>
